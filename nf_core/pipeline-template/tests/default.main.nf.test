nextflow_pipeline {

    name "Test pipeline with default settings"
    script "../main.nf"
    // tag  {{ name }}
    tag "PIPELINE"

    test("Default profile test") {

        when {
            params {
                outdir = "$outputDir"
            }
        }

        then {
            assertAll(
                { assert workflow.success }
            )
        }
    }
}
