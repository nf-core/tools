nextflow_pipeline {

    name "Test Workflow main.nf"
    script "main.nf"

    test("default test config") {
        when {
            params {
                outdir = "tests/results"
                outdir = "$outputDir"
            }
        }

        then {
            assert workflow.success
        }
    }

}
