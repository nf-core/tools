markdown_intro = """
# What scheduler does the HPC use?

Multi-node HPCs typically have software called a 'scheduler' that manages the allocation of resources to jobs.

By telling Nextflow which scheduler the HPC uses ('executor' in Nextflow terms), it can submit jobs to the scheduler and manage the resources for you.
"""
