## TODO nf-core: Please run the following command to build this file:
#                nf-core subworkflows create-test-yml {{ subworkflow_name_underscore }}
- name: "{{ subworkflow_name_underscore }}"
  command: nextflow run ./tests/modules/{{ subworkflow_dir }} -entry test_{{ subworkflow_name_underscore }} -c ./tests/config/nextflow.config -c ./tests/modules/{{ tool_dir }}/nextflow.config
  tags:
    - "{{ tool }}"
    # {%- if subtool %}
    - "{{ tool }}/{{ subtool }}"
    # {%- endif %}
  files:
    - path: "output/{{ tool }}/test.bam"
      md5sum: e667c7caad0bc4b7ac383fd023c654fc
    - path: output/{{ tool }}/versions.yml
      md5sum: a01fe51bc4c6a3a6226fbf77b2c7cf3b
