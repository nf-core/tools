# bash_login

# variables
hn=$(hostname)

# ASCII art image
cat << EOF
      _~
  _~ )_)_~      
  )_))_))_)     $hn
  _!__!__!_     $USER
  \______t/
~~~~~~~~~~~~~
EOF

if [ -f ~/.bashrc ]; then 
    source ~/.bashrc 
fi