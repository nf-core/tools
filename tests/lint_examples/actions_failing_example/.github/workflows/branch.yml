name: nf-core branch protection
jobs:
  test:
    runs-on: ubuntu-18.04
    steps:
      # PRs are only ok if coming from an nf-core `dev` branch or a fork `patch` branch
      - name: Check PRs
        run: bad example
      - name: Check sth
        run: still bad example
